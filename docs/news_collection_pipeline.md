
```mermaid
graph TD
    A[شروع وظیفه جمع‌آوری<br>(Celery Task)] --> B[اتصال به منابع خارجی<br>(APIها، وب‌اسکرپینگ)]
    B --> C{دریافت اخبار خام}
    C --> D[پیش‌پردازش اولیه<br>(استخراج منبع، محتوا، تاریخ انتشار)]
    D --> E[اتصال به پایگاه داده<br>(PostgreSQL - DBManager)]
    E --> F[ذخیره در جدول raw_news]
    F --> G[اتمام وظیفه]
    E -->|خطا| H[ثبت خطا<br>(Logging)]
    H --> G
```
